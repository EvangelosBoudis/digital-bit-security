<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="5dp"
    android:layout_marginBottom="5dp"
    android:layout_marginStart="0dp"
    android:layout_marginEnd="10dp"
    android:paddingTop="5dp"
    android:paddingBottom="5dp"
    android:paddingStart="20dp"
    android:paddingEnd="7.5dp"
    tools:background="@drawable/tag_shape">

    <TextView
        android:id="@+id/description_field"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/remove_btn"
        android:textSize="14sp"
        android:fontFamily="@font/breeze_sans_regular"
        android:textColor="@android:color/white"
        tools:text="Social"/>

    <ImageView
        android:id="@+id/remove_btn"
        android:layout_height="35dp"
        android:layout_width="35dp"
        android:padding="12.5dp"
        app:layout_constraintTop_toTopOf="@id/description_field"
        app:layout_constraintBottom_toBottomOf="@id/description_field"
        app:layout_constraintStart_toEndOf="@id/description_field"
        app:layout_constraintEnd_toEndOf="parent"
        app:tint="@android:color/white"
        tools:ignore="ContentDescription"/>

</androidx.constraintlayout.widget.ConstraintLayout>